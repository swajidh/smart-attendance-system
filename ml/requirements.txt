# ML package dependencies
# facenet-pytorch requires numpy<2.0 — install manually on a compatible env:
#   pip install "numpy<2.0" facenet-pytorch
# Until then the fallback MobileNetV3 encoder (torchvision) is used automatically.

torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.9.0
numpy>=1.24.0
mediapipe>=0.10.9
onnxruntime>=1.17.0
Pillow>=9.0.0
ultralytics>=8.0.0
